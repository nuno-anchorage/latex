% blockchainpt.sty
% !TEX program = xelatex
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{blockchainpt}[2025/04/10 Template para entregáveis Blockchain.PT com Nunito]

\usepackage{fontspec} % Para usar fontes do sistema
\setmainfont{Nunito}[
  Path = ../fonts/Nunito/static/,
  Extension = .ttf,
  UprightFont = *-Regular,
  BoldFont = *-Bold,
  ItalicFont = *-Italic,
  BoldItalicFont = *-BoldItalic
]

\usepackage{settings}

\usepackage[portuguese]{babel}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{pageslts}
\usepackage{tabularx}
\usepackage{array}
\usepackage[backend=biber,style=alphabetic]{biblatex}

\addbibresource{bibliography.bib}

\geometry{top=1.97in, bottom=1.18in, left=0.98in, right=0.98in, headheight=50pt, headsep=20pt}
\pagestyle{fancy}

% --- CABEÇALHOS E RODAPÉS ---
\fancyhf{}
\renewcommand{\headrulewidth}{0pt} % Remover a linha abaixo do cabeçalho
\renewcommand{\footrulewidth}{0pt} % Remover a linha acima do rodapé (se houver)

\pagenumbering{arabic}

\fancypagestyle{firstpage}{
  \fancyhf{} % limpa cabeçalho e rodapé
  \fancyhead[L]{
    \includegraphics[width=2.63in]{../images/Logotipo agenda.png}%
  }
  \fancyfoot[L]{
    \raisebox{-0.35scm}{\includegraphics[width=2.42in]{../images/Barra Logotipos PRR Cores.png}} % imagem à esquerda no rodapé
  }
  \fancyfoot[R]{\small Página \thepage\ de \pageref{LastPage}} % página X de Y
}

\fancypagestyle{otherpages}{
  \fancyhf{} % limpa cabeçalho e rodapé
  \fancyhead[L]{
    \includegraphics[width=1.88in]{../images/Logotipo agenda.png}%
  }
  \fancyfoot[L]{
    \raisebox{-0.35cm}{\includegraphics[width=2.42in]{../images/Barra Logotipos PRR Cores.png}} % imagem à esquerda no rodapé
  }
  \fancyfoot[R]{\small Página \thepage\ de \pageref{LastPage}} % página X de Y
}

\pagestyle{otherpages}

\thispagestyle{firstpage}

% Título personalizado
\renewcommand{\maketitle}{
  \begin{flushright}
    \begin{minipage}{0.7\textwidth}
      \textbf{\small{Blockchain.PT – Descentralizar Portugal com Blockchain}}\\
      \textbf{\small{ENTREGÁVEL}}
    \end{minipage}
  \end{flushright}
  \vspace{10cm}
}

\newcommand{\entregavel}{
  \begin{center}
    \large{ENTREGÁVEL \numeroDoEntregavel - \nomeDoEntregavel}\\[3cm]
    \large{\textbf{\dataDoEntregavel}}
  \end{center}
}
